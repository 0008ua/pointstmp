import { StatBaseDirective } from './stat.directive';

describe('StatBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new StatBaseDirective();
    expect(directive).toBeTruthy();
  });
});
